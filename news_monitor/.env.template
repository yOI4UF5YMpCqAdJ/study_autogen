# OpenAI配置
OPENAI_API_KEY=your_openai_api_key
OPENAI_API_BASE=https://api.deepbricks.ai/v1/
OPENAI_MODEL=gpt-4o-mini

# Firecrawl API配置
FIRECRAWL_API_KEY=your_firecrawl_api_key
FIRECRAWL_API_URL=http://localhost:3002

# 126邮箱配置
# SMTP服务器: smtp.126.com
# SMTP端口: 465 (SSL)或994 (SSL)
# 注意: 密码需要使用授权码，不是邮箱密码
# 获取授权码步骤：
# 1. 登录126邮箱网页版
# 2. 点击"设置" -> "POP3/SMTP/IMAP"
# 3. 开启"POP3/SMTP服务"
# 4. 获取授权码
SMTP_SERVER=smtp.126.com
SMTP_PORT=465
SMTP_USERNAME=your_username@126.com  # 完整的126邮箱地址
SMTP_PASSWORD=your_authorization_code  # 授权码，不是邮箱密码
RECIPIENT_EMAIL=recipient@example.com  # 接收新闻的邮箱地址

# 新闻监控配置
CHECK_INTERVAL=3600  # 检查间隔（秒）
MAX_NEWS_PER_BATCH=10  # 每批次最大新闻数量
